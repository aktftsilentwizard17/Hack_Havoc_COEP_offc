services:
  - type: web
    name: oceanembed-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python start_backend.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
